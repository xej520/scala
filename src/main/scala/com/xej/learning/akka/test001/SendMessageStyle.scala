package com.xej.learning.akka.test001

import akka.actor

/**
  * 以消息传递为核心的设计:使用Actor的actor方法，使用不可变对象，不考虑数据共享问题，以消息传递为设计核心
  */
//import actors._
//import akka.actor.Actor._
object SendMessageStyle {
  def main(args: Array[String]): Unit = {
//    val caller = self
//    val accumulator = actor {
//      var continue = true
//      var sum = 0
//      loopWhile (continue) {
//
//      }
//    }


  }
}
